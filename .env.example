# 复制成 .env 再填（.env 已在 .gitignore 里，不会进仓库）。
#
# 进程启动时由 src/sourcepilot/settings.py 自己读进环境变量，所以 IDEA 启动、
# 命令行 uvicorn、cron 三种起法拿到的是同一份配置，不依赖 IDE 装了什么插件。
# 真实环境变量优先——部署时 export 的值会覆盖这里。
#
# ⚠️ 凭据只放这里，**不要写进 .idea/runConfigurations/*.xml**：那些文件是跟着
# 仓库走的，写进去等于直接推上 GitHub。

# ── 采集中断告警（见 README「采集中断告警」）──────────────
# 与 AIRADAR 的 telegram_notifier 同名，同一个机器人可以直接复用。
# 取法：跟 @BotFather 建机器人拿 token；chat_id 找 @userinfobot 要，
# 或者先给机器人发一句话再打 https://api.telegram.org/bot<token>/getUpdates。
# 两个都留空 = 不推送，其余功能不受影响。
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# ── 其余可选项，一般不用动 ────────────────────────────
# SOURCEPILOT_DB=data/sourcepilot.db
# SOURCEPILOT_LIVE_TIMEOUT=8
# fake-ip 段放行（Clash 这类代理需要，见 README 已知边界）。设空 = 严格模式。
# SOURCEPILOT_FAKE_IP_CIDRS=198.18.0.0/15,fdfe:dcba:9876::/48
